[project]
name = "workshop"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.1",
    "transformers==5.7.0",
    "torch==2.9.0",
    "jupyterlab>=4.6.3,<5",

]

[dependency-groups]
dev = [
    "pillow>=12.3.0",
    "ipywidgets"
]
